version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Europe/Moscow"
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps:dev)"
      include: "scope"
    groups:
      dev-dependencies:
        patterns: ["*"]
        dependency-type: "development"
      production-dependencies:
        patterns: ["*"]
        dependency-type: "production"
    labels:
      - "dependencies"
      - "automerge-candidate"
    # Auto-merge for patch updates only (configured via GitHub branch protection)
    # Requires: "Allow auto-merge" enabled in repo settings + dependabot labels
    auto-merge:
      # Only auto-merge if all status checks pass
      allowed: true
    ignore:
      - dependency-name: "next"
        versions: ["15.x"]  # Major version pins handled manually
      - dependency-name: "react"
        versions: ["19.x"]
      - dependency-name: "react-dom"
        versions: ["19.x"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Europe/Moscow"
    commit-message:
      prefix: "chore(ci)"
    labels:
      - "ci"
      - "dependencies"