# API endpoint root — proxied to backend in dev, absolute in production
# VITE_ prefix makes it available at import.meta.env in Vite
# Default: the API served from the same origin as the frontend (e.g. https://compmasone.ru/api)
VITE_API_BASE=/api
